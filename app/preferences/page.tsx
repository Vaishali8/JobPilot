/**
 * Preferences page — where the user configures their job search criteria.
 *
 * This is a placeholder that will be built out in Step 2.
 * The full form will include: roles, employment types, location types,
 * cities, posting age, additional preferences, and resume upload.
 */
export default function PreferencesPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold tracking-tight text-foreground">
        Preferences
      </h1>
      <p className="mt-2 text-muted-foreground">
        Configure your job search criteria and upload your resume.
      </p>

      {/* Placeholder — the full PreferenceForm component will go here */}
      <div className="mt-8 rounded-lg border border-dashed border-border p-12 text-center text-muted-foreground">
        Preference form will be built in Step 2.
      </div>
    </div>
  );
}
