/**
 * Jobs dashboard page — displays job listings scored against the user's resume.
 *
 * This is a placeholder that will be built out in Steps 3-5.
 * The full page will show a filterable, sortable grid of job cards
 * with match scores and action buttons.
 */
export default function JobsPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold tracking-tight text-foreground">
        Job Listings
      </h1>
      <p className="mt-2 text-muted-foreground">
        Jobs matched and scored against your resume.
      </p>

      {/* Placeholder — JobCard grid and filters will go here */}
      <div className="mt-8 rounded-lg border border-dashed border-border p-12 text-center text-muted-foreground">
        Job listings dashboard will be built in Steps 3-5.
      </div>
    </div>
  );
}
