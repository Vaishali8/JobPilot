/**
 * Application tracker page — shows all jobs the user has applied to.
 *
 * This is a placeholder that will be built out in Step 6.
 * The full page will include a stats bar, a table with inline editing
 * for status and notes, and the ability to add manual entries.
 */
export default function TrackerPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold tracking-tight text-foreground">
        Application Tracker
      </h1>
      <p className="mt-2 text-muted-foreground">
        Track and manage your job applications.
      </p>

      {/* Placeholder — TrackerTable and stats bar will go here */}
      <div className="mt-8 rounded-lg border border-dashed border-border p-12 text-center text-muted-foreground">
        Application tracker will be built in Step 6.
      </div>
    </div>
  );
}
